/*
 * AUTHOR	: Hydrolyzed~
 * SCHOOL	: RYW
 * TASK		: AG_SARS
 * ALGO		:
 * DATE		:
 * */
#include<bits/stdc++.h>
using namespace std;

#define endl "\n"
#define all(x) (x).begin(), (x).end()

using ll = long long;

vector<int> x;

void solve(){
	
	return ;
}

int main(){
	cin.tie(nullptr)->ios::sync_with_stdio(false);
	int q = 1, n, k;
	cin >> n >> k >> q;
	for(int i=0, a; i<n; ++i){
		cin >> a;
		v.push_back(a);
	}
	for(int i=0; i<q; ++i){
		int a, b;
		cin >> a >> b;
		
	return 0;
}
